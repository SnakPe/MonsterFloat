{
  "name": "monsterfloat",
  "version": "1.0.6",
  "type": "module",
  "description": "Arbitrary-length rational numbers",
  "scripts": {
    "build": "tsc && rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "arbitrary-length"
  ],
  "main": "dist/bundled/monsterfloat.js",
  "types": "dist/bundled/MonsterFloat.d.ts",
  "files": [
    "/dist/bundled"
  ],
  "author": "Snake",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^4.52.5"
  }
}
